# Variables de entorno requeridas
# Copiar este archivo a .env y ajustar los valores

# ==========================================
# APLICACIÓN
# ==========================================
NODE_ENV=development
PORT=3000

# ==========================================
# JWT - Seguridad
# ==========================================
# IMPORTANTE: Cambiar en producción por una clave segura
JWT_SECRET=tu-secret-super-seguro-cambiarlo-en-produccion-usa-openssl-rand-hex-32

# ==========================================
# BASE DE DATOS
# ==========================================
DB_HOST=localhost
DB_PORT=3306
DB_USERNAME=root
DB_PASSWORD=tu-password-aqui
DB_DATABASE=ghv_uip

# CRÍTICO: Nunca usar true en producción
# synchronize: true puede borrar datos
DB_SYNC=false

# ==========================================
# SEGURIDAD
# ==========================================
# Origen permitido para CORS (separar múltiples con comas)
# Desarrollo: http://localhost:5173
# Producción: https://tu-dominio.com
CORS_ORIGIN=http://localhost:5173

# Rate Limiting (límite de requests)
THROTTLE_TTL=60  # Tiempo en segundos
THROTTLE_LIMIT=100  # Máximo de requests en ese tiempo

# ==========================================
# ARCHIVOS
# ==========================================
# Tamaño máximo de archivo en bytes (5MB = 5242880)
MAX_FILE_SIZE=5242880

# Tipos de archivo permitidos (extensiones separadas por coma)
ALLOWED_FILE_TYPES=pdf,jpg,jpeg,png

# Directorio de almacenamiento de archivos
UPLOAD_DIR=./uploads

# ==========================================
# LOGGING
# ==========================================
# Niveles: error, warn, info, debug, verbose
LOG_LEVEL=info

# Directorio de logs
LOG_DIR=./logs

# ==========================================
# EMAIL (Nodemailer)
# ==========================================
# Para Gmail, habilitar "aplicaciones menos seguras" o usar App Password
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_SECURE=false  # true para 465, false para otros puertos
MAIL_USER=tu-email@gmail.com
MAIL_PASSWORD=tu-app-password-aqui
MAIL_FROM="Sistema GHV <noreply@uniputumayo.edu.co>"

# ==========================================
# REDIS (Opcional - para caché)
# ==========================================
# Descomentar si se implementa caché con Redis
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_PASSWORD=
# REDIS_TTL=3600  # Tiempo de vida del caché en segundos

# ==========================================
# URLs
# ==========================================
# URL del frontend (para links en emails)
FRONTEND_URL=http://localhost:5173

# URL del backend (para Swagger y enlaces)
BACKEND_URL=http://localhost:3000

# ==========================================
# FEATURES FLAGS (Opcional)
# ==========================================
# Habilitar/deshabilitar características
ENABLE_EMAIL_NOTIFICATIONS=true
ENABLE_PDF_REPORTS=true
ENABLE_FILE_UPLOAD=true
ENABLE_SWAGGER=true
